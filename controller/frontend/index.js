const sequelize = require('../../config/database');

const listingPage = async((res, req) => {
    try {
        console.log('listingPage');
    } catch (error) {
        throw error;
    }
});

module.exports = {
    listingPage
}